<form action="{{ path(route_category, { 'slug' : category.slug } ) }}" method="post" {#{ form_enctype(form) }#} class="clearfix">
    {{ form_widget(form) }}

    <div class="rest">
            <input type="submit" class="submit btn btn-sm btn-primary" />
    </div>
</form>
