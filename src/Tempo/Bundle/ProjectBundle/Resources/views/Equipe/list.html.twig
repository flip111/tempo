<ul class="list">
    {% for user in users %}
    <li><a href="{{ path('user_profile_show', { 'slug': user.slug }) }}"> <img src="{{ user.avatar(60) }}" />{{ user.userName }} </a></li>
    {% endfor %}
</ul>
